#include<bits/stdc++.h>

using namespace std;

int n, m, l, d;

int main() {
  scanf("%d%d%d%d", &n, &m, &l, &d);
  while (1) {
    puts("reach_dest");
    fflush(stdout);
    scanf("%*d");
    puts("move_left");
    fflush(stdout);
    scanf("%*d");
  }
  return 0;
}
